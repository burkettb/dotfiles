# Pagination

Numbered pages with prev/next and collapsed ellipses. 1-indexed.

```jsx
<Pagination page={page} pageCount={24} onChange={setPage} />
```

`DataTable` has its own built-in pagination row — use this standalone version under card grids, blog lists, and search results.
