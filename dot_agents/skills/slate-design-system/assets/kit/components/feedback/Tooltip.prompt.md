# Tooltip

A small dark label on hover/focus for supplementary hints. Wrap a single trigger. Never hide essential info in a tooltip.

```jsx
<Tooltip label="Archive project">
  <IconButton icon="archive-box" label="Archive" />
</Tooltip>
```
