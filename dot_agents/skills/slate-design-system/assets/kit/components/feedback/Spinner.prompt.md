# Spinner

Indeterminate loading indicator for unknown-duration waits (submitting, fetching a single thing).

```jsx
<Spinner size="sm" label="Sending invoice" />
```

Known progress → `Progress`. Loading a whole region's structure → `Skeleton`. Never use a spinner as decoration.
