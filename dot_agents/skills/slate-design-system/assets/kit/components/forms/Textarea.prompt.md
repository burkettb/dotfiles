# Textarea

Outlined multi-line field. Same label / helper / error layout as Input.

```jsx
<Textarea label="Description" rows={5} placeholder="What does this project do?" />
<Textarea label="Notes" error="Notes can't be empty." />
```
