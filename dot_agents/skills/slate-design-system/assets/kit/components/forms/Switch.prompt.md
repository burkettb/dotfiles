# Switch

Binary toggle for immediate on/off settings (not form submission). Label to the right.

```jsx
<Switch label="Enable two-factor authentication" defaultChecked />
<Switch label="Public project" />
```
