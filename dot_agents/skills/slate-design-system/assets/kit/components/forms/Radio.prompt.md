# Radio

Round single-choice control. Give every radio in a group the same `name`.

```jsx
<Radio name="billing" label="Monthly" defaultChecked />
<Radio name="billing" label="Annual (save 20%)" />
```
