-- Machine-specific plugin configuration
-- Copy this file to local.lua and customize for your specific machine
-- local.lua is gitignored, so it won't sync between machines

return {
  -- EXAMPLE: Personal machine with local manim development
  -- {
  --   dir = "~/projects/custom-manim-plugin",
  --   keys = {
  --     { "<leader>mp", "<cmd>ManimPreview<CR>", desc = "Manim Preview" },
  --   },
  -- },

  -- EXAMPLE: Work machine with company-specific tools
  -- {
  --   'company/internal-cfml-tools',
  --   ft = { 'cfml', 'cfm', 'cfc' },
  --   config = function()
  --     -- Company-specific ColdFusion configuration
  --   end,
  -- },

  -- EXAMPLE: Machine-specific LSP servers
  -- {
  --   'neovim/nvim-lspconfig',
  --   opts = function(_, opts)
  --     -- Add machine-specific LSP servers here
  --     return opts
  --   end,
  -- },
}